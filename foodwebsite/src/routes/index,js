import { createBrowserRouter } from "react-router-dom";
import { Home } from "../pages";


const _ROUTERS = createBrowserRouter([
    {
        path: '/',
        element: <Home/>,
        children: [{
            path: './'
        }
        ]
    }
])